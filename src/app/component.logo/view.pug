.d-flex
    .logo.col
        h2(
            (dragend)="prevent($event)",
            (dragstart)="prevent($event)",
            (dragenter)="prevent($event)",
        ) Cellytics NK

    .window-close.z-3.d-inline-block.cursor-pointer(
        [style.opacity]="opacity",
        (mouseenter)="opacity=1;render();",
        (mouseleave)="opacity=0;render();",
        (click)="close()",
    ) &#215;