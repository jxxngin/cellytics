# Cellytics-NK

```shell
cd build
npm install -D electron electron-builder
npm install concurrently
```

## src/angular/package.json

- copy scripts, build to `build/package.json`

## RUN

```shell
cd build
npm run el:start
```

## BUILD

```shell
cd build
npm run el:build --win
```
